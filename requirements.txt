# Pinned for reproducible installs (L14). Compatible ranges kept only where
# platform wheels differ (TF/MediaPipe). Prefer exact pins for app deps.
flask==3.1.3
opencv-python==4.10.0.84
mediapipe==0.10.14
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.1
joblib==1.4.2
psutil==6.0.0
tensorflow==2.17.0
keras==3.5.0
