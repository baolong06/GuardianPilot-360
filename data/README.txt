# Thư mục này dùng để đặt file video input khi chạy Docker.
# Đặt file video của bạn vào đây với tên: input.mp4
# Hoặc override bằng: -e VIDEO_PATH=/data/ten_file.mp4

# Ví dụ:
#   cp /path/to/driver_test.mp4 data/input.mp4
#   docker compose run --rm video
